{
  "name": "app-photo-manager",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "../node_modules/.bin/react-scripts start",
    "build": "../node_modules/.bin/react-scripts build",
    "test": "../node_modules/.bin/react-scripts test --env=jsdom",
    "eject": "../node_modules/.bin/react-scripts eject"
  }
}
